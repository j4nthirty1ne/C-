#include <iostream>
using namespace std;

int main ()
{
    // int a = 2;
    // int *ptr = &a;
    // cout << "Address of a: " << ptr << endl;
    // cout << "Value of a: " << *ptr << endl;
    // cout << "Pointer of a: " << *ptr << endl;
    // cout << "Value after modified " << *ptr << endl;
    // *ptr = 5;
    // cout << "Address: " << ptr << endl;
    // cout << "Pointer of a: " << *ptr << endl;

    // int a = 2;
    // double b =10; 
    // void *p = &b;
    // double *ptr = static_cast<double *>(p);
    // cout << "a = " << *ptr;

    
    return 0;
}
